import styled from "styled-components";

export const Icon = styled.img`
  height: 20px;
  pointer-events: none;
  position: absolute;
  right: 12px;
  top: 50%;
  transform: translateY(-50%);
  width: 20px;
`;